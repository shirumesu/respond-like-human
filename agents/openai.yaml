interface:
  display_name: "Respond Like Human"
  short_description: "全局回复风格基线：自然中文、少翻译腔、少模板味、少说教"
  default_prompt: "Use $respond-like-human when any conversation start, as the default reply-style baseline."
policy:
  allow_implicit_invocation: true
